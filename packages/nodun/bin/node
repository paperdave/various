#!/bin/sh
if [ -z "$*" ]; then
  exec bun
fi
case "$1" in
  *.js|*.ts)
    exec bun "$1"
    ;;
  *)
    FILE=$(mktemp --suffix=.js)
    printf 'import "'"$1"'";' > "$FILE"
    bun "$FILE"
    rm "$FILE"
    ;;
esac
