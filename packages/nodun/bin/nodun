#!/bin/sh
if [ -z "$*" ]; then
  printf "\e[32;1mnodum\e[0m: tricks programs to run js with bun instead of node.js\n"
  exec bun
fi
NODUN_PATH=$(dirname $(realpath "$0"))
chmod +x $NODUNPATH/node
export PATH="$NODUN_PATH:$PATH"
exec bun "$@"
