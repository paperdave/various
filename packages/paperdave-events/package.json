{
  "name": "@paperdave/events",
  "version": "1.0.1",
  "description": "Another event emitter library, with easy TS types.",
  "keywords": [
    "events"
  ],
  "author": "Dave Caruso <dave@davecode.net>",
  "type": "module",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && esbuild --outfile=./dist/index.js --target=es2020 --format=esm --sourcemap --bundle src/index.ts",
    "lint": "eslint . --max-warnings=0"
  },
  "devDependencies": {
    "@paperdave/utils": "workspace:^1.0.3",
    "bun-types": "^0.1.2",
    "esbuild": "^0.14.39",
    "typescript": "^4.6.4"
  },
  "publishConfig": {
    "access": "public"
  }
}
